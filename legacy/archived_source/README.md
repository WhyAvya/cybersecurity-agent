# Archived Source

This directory preserves source files that were no longer part of the supported
execution path. They are stored as text to avoid accidental imports or execution
while retaining the original implementation for reference.
